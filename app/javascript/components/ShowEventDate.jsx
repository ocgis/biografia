import Show from './Show';
import EventDate from './EventDate';

class ShowEventDate extends Show {
  constructor(props) {
    super(props, EventDate, 'EventDate');
  }
}

export default ShowEventDate;
