<%= application_modal_dialog("Ny händelse") do %>
<% hidden_fields = {}
   hidden_fields['parentName'] = parentName if defined? parentName
   hidden_fields['topName'] = topName if defined? topName %>
<%= render partial: 'form', object: @event, locals: { hidden_fields: hidden_fields, options: { remote: true, url: { action: "createp" } } } %>
<% end %>
