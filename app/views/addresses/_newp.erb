<%= application_modal_dialog("Ny adress") do %>
<% hidden_fields = {}
   hidden_fields['parentName'] = parentName if defined? parentName
   hidden_fields['topName'] = topName if defined? topName %>
<%= render partial: 'form', object: @address, locals: { hidden_fields: hidden_fields, options: { remote: true, url: { action: "createp" } } } %>
<% end %>
