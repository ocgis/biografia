<%=
  new_options = options.merge({ :enclosedById => false,
                                :showModifier => false })
  render :partial => connchoose.controller + '/showp', :object => connchoose, :locals => { :options => new_options }
%>

<%= application_modal_dialog("Välj") do %>
<%= application_search_form(options.merge(submitAction: { :controller => 'references',
                                                           :action => "connection_add" },
                                          ignoreName: connchoose.object_name,
                                          hiddenFields: { connect1Id: connchoose.object_name })) %>
<% end %>

