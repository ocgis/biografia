  <%=h content_for(:title, 'Personinformation för ' + @object.short_name) %>
	<%= render :partial => "showp",
		:object => @object,
		:locals => { :options => { :showFull => true,
	                             :enclosedById => true,
				                       :showModifier => true } }
  %>
