  <% options =
      {
        :topName => @object.object_name,
        :showFull => true,
        :enclosedById => true,
        :showModifier => true,
        :mode => @mode
      }
  %>
  <%=h content_for(:title, 'Media: ' + @object.file_name) %>
	<%= render :partial => "media/showp",
		:object => @object,
		:locals => { :options => options }
  %>
