<%= form_for :media, :url => {:action=>:create}, :html=>{:multipart=>true} do |f| %>
	<%= radio_button_tag 'location', 'server', false, { :data => { :hideelement => 'ul_file_name' } } %> redan på server
	<br />
	<%= radio_button_tag 'location', 'upload', true, { :data => { :showelement => 'ul_file_name' } } %> ladda upp <%= file_field 'upload', 'file_name', { :id => 'ul_file_name' } %>
	<br />
	
	Filnamn:
	<%= f.text_field :file_name %>
	<br />
    <%= f.submit 'Lägg till' %>
<% end %>
