<%= application_modal_dialog("Nytt datum") do %>
<% hidden_fields = {}
   hidden_fields['parentName'] = parentName if defined? parentName
   hidden_fields['topName'] = topName if defined? topName %>
<%= render partial: 'form', object: @event_date, locals: { hidden_fields: hidden_fields, options: { remote: true, url: { action: "createp" } } } %>
<% end %>
