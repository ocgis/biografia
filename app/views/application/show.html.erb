  <%=h content_for(:title, 'Information om ' + @object.decorate.one_line) %>
	<%= render :partial => "showp",
		:object => @object,
		:locals => { :options => { :topName => @object.object_name,
		                           :showFull => true,
	                             :enclosedById => true,
				                       :showModifier => true } }
  %>
