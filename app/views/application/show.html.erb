  <%=h content_for(:title, 'Information om ' + @object.one_line) %>
	<%= render :partial => "showp",
		:object => { :object => @object,
                             :related_objects => @related },
		:locals => { :options => { :showFull => true,
	                                   :enclosedById => true,
				           :showModifier => true } }
  %>
