<%=
  new_options = options.merge({ :enclosedById => false,
                                :showModifier => false })
  render :partial => selmerge.controller + '/showp', :object => selmerge, :locals => { :options => new_options }
%>

<%= application_modal_dialog("Välj") do %>
<%= application_search_form(options.merge(submitAction: { :controller => selmerge.controller,
                                                          :action => "edmerge" },
                                          ignoreName: selmerge.object_name,
                                          searchModel: selmerge.class.name,
                                          hiddenFields: { connect1Id: selmerge.object_name })) %>
<% end %>

